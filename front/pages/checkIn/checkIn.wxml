<!--pages/checkIn/checkIn.wxml-->
<view class="main">
    <view class="course-now">{{courseNow}}</view>
    <button
        class="check-button"
        loading="{{buttonLoading}}"
        disabled="{{buttonDisable}}"
        bindtap="onTap"
    >
        {{buttonText}}
    </button>
</view>
